model Utilities {
  id         Int      @id @default(autoincrement())
  name       String
  money      Float
  business   Business @relation(fields: [businessId], references: [id])
  businessId Int
}
