model Student {
  id          Int           @id @default(autoincrement())
  name        String
  sex         String
  age         Int
  money       Float         @default(0.00)
  dormitory   String
  password    String
  Address     Address[]
  transaction Transaction[]
  orders      Order[]
}
